import 'package:flutter/material.dart';

class DetailsCharacters extends StatelessWidget {
  const DetailsCharacters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
